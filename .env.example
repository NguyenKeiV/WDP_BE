# Server Configuration
NODE_ENV=development
PORT=3000

# PostgreSQL Database Configuration

# Option 2: Use individual database variables (for local development)
DB_HOST=103.228.36.168
DB_PORT=5432
DB_USERNAME=db_user
DB_PASSWORD=db_password
DB_DATABASE=postgres

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=7d

# CORS Configuration
FRONTEND_URL=http://localhost:5173

# Application Settings
BCRYPT_ROUNDS=12

# SMTP Mail Configuration (for sending login credentials to team leaders)
# Gmail: host=smtp.gmail.com, port=587, secure=false (use App Password, not your real password)
# Outlook: host=smtp-mail.outlook.com, port=587, secure=false
# Or use a service like Mailgun, SendGrid, etc.
MAIL_HOST=smtp.gmail.com
MAIL_PORT=587
MAIL_SECURE=false
MAIL_USER=hungnkse180346@fpt.edu.vn
MAIL_PASS=votnyhvkwivvtwms
MAIL_FROM_NAME=WDP Food Relief
MAIL_FROM_EMAIL=noreply@wdpfoodrelief.com
